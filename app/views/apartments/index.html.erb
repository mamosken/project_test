

<div>
	<%= form_tag apartments_path, method: 'get' do %>
		<p>
			<%= text_field_tag :search, params[:search] %>
			<%= submit_tag "search", name: "nil" %>
		</p>
	<% end %>
</div>
	<div>
<%if @rooms %>
	<% @rooms.each do |room| %>
	<p> You searched for keywork <u><i><%= room.name%><i></u><p>
	</div>
<% end %>

<% @apartment.each_slice(3) do |apartments| %>
	<div class="row">
	    <% apartments.each do |apartment| %>
		    <div class= "col-md-4">
	    		<div class="apartment">
		     		 <div class= "image_wrapper">
				       	<%= link_to apartment do  %>
	
				       	<h4> <%= link_to apartment.title, apartment %></h4>
			       		<%= link_to (image_tag apartment.image.url(:medium, class: "apartment_image")), apartment  %></br>
			       		<p class="bellow_image_contact">posted by </p> 
		     	 	</div>
	 			</div>
			</div>	
					
			<% end %>
<!-- 	</div>
	</div>	 -->
		<% end %>
    	
	
	</div>
<% end %>
<% else %>
<% @apartment.each_slice(3) do |apartments| %>
	<div class="row">
	    <% apartments.each do |apartment| %>
	    	
		    <div class= "col-md-4">
	    		<div class="apartment">
		     		 <div class= "image_wrapper">
				       	<%= link_to apartment do  %>
				       	<h4> <%= link_to apartment.title, apartment %></h4>
			       		<%= link_to (image_tag apartment.image.url(:medium, class: "apartment_image")), apartment  %></br>
			       		<p class="bellow_image_contact">posted by </p> 
		     	 	</div>
	 			</div>
			</div>	
					<% end %>
			<% end %>
	</div>
<% end %>
<% end %>
